# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
.artifacts/
*.artifacts


# Local port / env overrides.
# .env.* is a wildcard on purpose: .env.production and .env.production.local
# are the files that end up holding real keys, and listing only .env/.env.local
# left those two tracked by default.
.env
.env.*
!.env.example

# Scratch: files moved aside instead of deleted, and test screenshots
_to_delete/
shots/

# Local helper, not part of the project
push-to-github.bat
